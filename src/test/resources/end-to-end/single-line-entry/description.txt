An entry that fits on one line, such as a @preamble, is recognized as complete, so the text after
it is removed.  This entry's value contains nested braces and a backslash-escaped brace, neither of
which ends the entry early.
