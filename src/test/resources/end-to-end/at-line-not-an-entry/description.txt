The program treats any line that starts with "@" as an entry, even when it is not.  Such a line
opens no delimiter, so it is an entry all by itself: the line is retained, but the text after it is
not.
